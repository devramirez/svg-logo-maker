// constructor function and parent class that will extend to children classes
class Shape {
    constructor(shapeColor, test, textColor) {
        this.shapeColor = shapeColor;
        this.text = test;
        this.textColor = textColor;
    }
}